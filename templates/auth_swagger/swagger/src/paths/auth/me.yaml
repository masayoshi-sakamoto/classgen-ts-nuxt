get:
  tags:
    - Auth
  operationId: authMe
  description: Auth Me
  responses:
    '200':
      description: OK
      content:
        application/json:
          schema:
            required:
              - <%= classname %>
            type: object
            properties:
              <%= classname %>:
                $ref: '#/components/schemas/<%= className %>'
